import { Status } from "../types/status.type";

export interface Hoa {
    // initial_filing_date: Date;
    status: string;
    // standing_sos: string;
    // standing_ftb: string;
    // standing_agent: string;
    formed_in: string;
    mailing_address: string;
    // principal_address: string;
    agent_name?: string;
    // adent_address?: string;
    id: string;
    name: string;
}

